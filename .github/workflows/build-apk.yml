name: Build Android APK (PWA)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  # Job 1: Build PWA Static Assets (Vite)
  build-pwa:
    name: Build PWA Assets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js v20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build PWA Bundle
        run: npm run build

      - name: Upload Static Assets
        uses: actions/upload-artifact@v4
        with:
          name: pwa-dist-assets
          path: dist/

  # Job 2: Microsoft PWABuilder Cloud Packaging (Recommended & Simplest)
  # Generates fully signed production .apk & .aab directly using the web manifest.
  pwabuilder-compiler:
    name: Microsoft PWABuilder APK
    needs: build-pwa
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Compile APK via PWABuilder Action
        uses: pwa-builder/pwa-action@v1
        continue-on-error: true
        with:
          pwa-url: "https://einundzwanzig-pool.de" # Replace with your live URL when deployed
          android-package-id: "de.einundzwanzig.pool"
          android-app-name: "EINUNDZWANZIG POOL"
          android-launcher-name: "EINUNDZWANZIG POOL"

      - name: Upload PWABuilder Output Packages
        uses: actions/upload-artifact@v4
        with:
          name: android-apk-pwabuilder
          path: ./*

  # Job 3: Local Offline Bubblewrap Compilation (Compiles directly on the GitHub runner)
  local-bubblewrap-compiler:
    name: Local Offline Bubblewrap APK
    needs: build-pwa
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Bubblewrap CLI
        run: npm install -g @bubblewrap/cli

      - name: Create local twa-manifest.json
        run: |
          cat << 'EOF' > twa-manifest.json
          {
            "packageId": "de.einundzwanzig.pool",
            "host": "einundzwanzig-pool.de",
            "name": "EINUNDZWANZIG POOL",
            "launcherName": "EINUNDZWANZIG POOL",
            "displayMode": "standalone",
            "themeColor": "#111827",
            "navigationColor": "#111827",
            "backgroundColor": "#111827",
            "enableNotifications": true,
            "startUrl": "/",
            "iconUrl": "https://einundzwanzig-pool.de/bitcoin-logo-512.png",
            "maskableIconUrl": "https://einundzwanzig-pool.de/bitcoin-logo-512.png",
            "splashScreenFadeOutDuration": 300,
            "signingKey": {
              "path": "./android.keystore",
              "alias": "android-key"
            },
            "appVersionCode": 1,
            "appVersion": "1.0.0",
            "fallbackType": "customtabs",
            "features": {},
            "alphaHeaders": []
          }
          EOF

      - name: Generate Signing Keystore
        run: |
          keytool -genkey -v \
            -keystore android.keystore \
            -alias android-key \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass password123 \
            -keypass password123 \
            -dname "CN=Android,O=Android,C=US"

      - name: Configure Bubblewrap Paths
        run: |
          mkdir -p ~/.bubblewrap
          echo "{\"jdkPath\":\"$JAVA_HOME\",\"androidSdkPath\":\"$ANDROID_SDK_ROOT\"}" > ~/.bubblewrap/config.json

      - name: Initialize and Validate Bubblewrap Project
        run: |
          bubblewrap init --manifest=twa-manifest.json --directory=./android-project --skipValidation

      - name: Build Signed Android APK & AAB
        working-directory: ./android-project
        run: |
          bubblewrap build --signingKeyPath=../android.keystore --signingKeyPassword=password123 --signingKeyAlias=android-key --signingKeyAliasPassword=password123

      - name: Upload Locally Compiled APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk-local-bubblewrap
          path: ./android-project/app-release-signed.apk
